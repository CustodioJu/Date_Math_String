1. Manipulação de Datas e Horas:
 Obter Hora Atual: Desenvolva uma função obterHoraAtual() que retorne a hora atual no formato HH:MM:SS.

// Cria uma nova instância do objeto Date, que armazena a data e hora atuais.
let data = new Date();

// Função para obter a hora atual formatada
function obterHoraAtual() {
  // O método toLocaleTimeString com o parâmetro "pt-BR" retorna a hora formatada no padrão brasileiro
  return (`${data.toLocaleTimeString("pt-BR")}`);
}

// Exibe a hora atual formatada no console
console.log(obterHoraAtual());
